# Block proxy 验证报告（Phase 1，E0–E8）

- 日期：2026-09-25
- 规格：[BLOCK_PROXY_VALIDATION_SPEC.md](BLOCK_PROXY_VALIDATION_SPEC.md)
- 代码：`block_proxy_validation.py`、`summarize_block_proxy_validation.py`（commit `a4133a5`）
- 结果目录：`results/audits/block_proxy_validation_20260925_002859/`（远端同名目录，另有 `_report.zip`）
- 性质：全部为已有 capture 上的离线计算，未运行新的模型前向，也不涉及任务准确率。

**单位约定**
- **pp**：注意力质量百分点，相对共享块 oracle C*。
- **rel_err**：单个 head 输出的相对误差 ‖O_d−O_s‖/‖O_d‖。
- **oproj**：经过 `o_proj` 后的相对误差。

## 0. 覆盖范围与校验

| 数据 | 样本 | 层 | 说明 |
| --- | --- | --- | --- |
| RULER 32K NIAH | MK1:56、MK1:71、MQ:59 | 0/7/14（3 条）、21/27（2 条） | 三条共用同一段 Paul Graham 背景文本，不能算独立重复 |
| LongBench v2 | 8 条（4 类文本 × 2，选样时不看答题结果） | 0/14/27 | 其中 7 条是本次补跑的 capture |

- 主配置：Q=K=128，远端预算 2048 token（16 块），sink/local 为 256/384。
- 每个 Q tile 采样 16 行，tile 位于上下文 25/50/75/90% 处；统计覆盖全部 28 个 Q 头。

**E0 复现**：原 4 条样本上 16 个 (panel, 层, 方法) 组合全部通过，与原报告的最大偏差约 3×10⁻⁶ pp。

**§6 校验全部通过**（见 `checks.json`）：

| 校验 | 实测最大值 | 阈值 |
| --- | --- | --- |
| 按块拼出的 dense 输出 vs 直接计算 | 5.3e-15 | 1e-5 |
| 单块删除公式 vs 直接重算（仅 P_b ≥ 1e-6 的行） | 3.6e-5 | 1e-4 |
| 频带 logit 求和 vs 总 logit | 5.5e-12 | 1e-4 |
| E2 恒等式 | 2.5e-12 | 1e-9 |
| RULER needle 答案定位 | 13/13 全部定位 | — |

**两处偏离 spec，均已说明：**
1. **RoPE 校验阈值。** spec 原阈值是 max abs < 1e-3。bf16 在 |k|≈400 处做不到：实测只有 1–3×10⁻⁴ 的元素有差异，差值 0.0078–0.125。经你同意，改为「差值 ≤ 输入对量级的 2 个 bf16 ulp，且不一致比例 < 1e-3」。同时记录了对照：换成错误的交错配对约定，误差为 30–905。
2. **单块删除校验只统计块质量 ≥ 1e-6 的行。** 质量更小时，直接重算的差值低于 float64 的分辨率，相对误差会变成 inf。

## 1. 结论总览

| 问题 | 结论 | 关键数字（L14，RULER / LongBench） |
| --- | --- | --- |
| Q1 COBS 二阶项能否修复漏选 | **不支持** | `mean_var2` regret 7.48 / 9.54 pp，MeanPool 为 7.53 / 7.89，真实块指数和为 0.86 / 1.98 |
| Q2 稀释还是抵消 | 按 spec 判据为**「抵消」**；机制上更准确的描述见 §3 | missed 的 A_minus 比 false_pos 高 0.64 / 0.57，CI 不含 0；RoPE 高频**不参与**，Δ 的 98% 来自低频带 |
| Q3 尖峰是否 sink 类 | **L14 支持，其他层不支持** | 尖峰位置中 sink 类占 7/7 / 10/16；这些 token 的 v 范数排在最低 0.02% |
| Q4 对输出的影响 | 预测**不成立**：输出误差没有明显小于质量损失所暗示的量级 | M 的 rel_err 中位数 0.139 / 0.142，C* 为 0.067 / 0.080；sink 块每单位质量造成的误差最低 |
| Q5 RBS 能否捞回 | **不支持** | 补救块中约 90% 不在 C*；三个已解码案例的补救分支都没捞回 |
| Q6 needle 块会不会被漏 | 固定预算下**会**；但 V1 在问题行上是 dense，所以不影响答题 | needle 块进 C* 65%，进 MeanPool 16%，进 V1 式阈值 7% |

## 2. Q1：COBS 二阶上界（E1）

Q=K=128 下相对 C* 的 regret（pp）。`mean_var2` 用的是精确的块内 logit 方差，是任何 COBS 式压缩的上界：

| 数据/层 | MeanPool | mean_var2 | 真实块指数和 |
| --- | ---: | ---: | ---: |
| RULER L0 | 1.93 | 1.99 | 1.56 |
| RULER L14 | 7.53 | 7.48 | 0.86 |
| RULER L27 | 0.78 | 0.61 | 0.33 |
| LongBench L0 | 2.18 | 2.31 | 1.77 |
| LongBench L14 | 7.89 | **9.54** | 1.98 |
| LongBench L27 | 1.51 | 2.12 | 0.59 |

- **L14 的 K 块大小扫描**（K = 32 / 64 / 128 / 256）：`mean_var2` 只在 RULER K=32 时明显改善（8.29 → 5.81），其余配置都没有稳定收益。

**原因：二阶项在两类块上的误差方向相反。** 下表比较 ½Var 与真实 Jensen gap g（L14）：

| 块类型 | ½Var > g 的块占比（RULER / LongBench） | 含义 |
| --- | --- | --- |
| hit / false_pos | 80–90% | 二阶项高估，把错误的块也抬了上去 |
| missed | 33% / 57% | 二阶项低估了尖峰块 |

**累积量级数在 missed 块上发散。** L14 missed 块中 |K₄−真值| > |K₂−真值| 的比例为 58% / 74%，hit 块为 32% / 50%。典型 missed 块：
- RULER：N_eff 中位数 1.6，top-1 份额 0.85，g = 6.1；
- LongBench：N_eff 3.4，top-1 0.58，g = 5.9。

单 token 尖峰本来就不适合用低阶累积量描述。两条预测（regret 比真实指数和高 5 pp 以上；missed 块部分和不收敛）**都成立**。

## 3. Q2：稀释还是抵消，RoPE 是否参与（E2–E4）

**E2（以选择边界 c 为参考；以候选块均值中位数为参考时结论相同）**

| L14 分组 | A_plus | A_minus | μ−c | 去掉 top-1 后 μ−c | top-1 在 exp 质量中的份额 |
| --- | ---: | ---: | ---: | ---: | ---: |
| RULER hit | 2.80 | 0.20 | 2.55 | 2.50 | 0.25 |
| RULER missed | 1.70 | **0.75** | 0.95 | 0.88 | **0.85** |
| RULER false_pos | 2.34 | 0.33 | 1.98 | 1.94 | 0.23 |
| LongBench missed | 2.46 | **0.72** | 1.68 | 1.61 | 0.58 |
| LongBench false_pos | 2.87 | 0.31 | 2.48 | 2.44 | 0.25 |

- **统计检验**（missed 减 false_pos，按 tile 重采样，95% CI）：A_minus 的差为 RULER 0.64 [0.58, 0.69]（Cliff's δ = 0.63）、LongBench 0.57 [0.54, 0.60]（δ = 0.51）。按 spec 判据，结论为**「抵消」**。L7/L21/L27 与 LongBench L0 的结果方向相同；RULER L0 不确定。
- **spec 稀释判据的第二条（A_plus 主要来自 top-1）在所有层都不成立**：top-1 在 A_plus 中只占 3–6%。原因是 A_plus 在线性 logit 空间计算，单个 token 在块均值里的权重只有 1/128，所以这一条对 exp 空间的尖峰天然不敏感。
- **我的解读（推断）**：missed 块是「低背景 + 单个尖峰」。背景 token 普遍低于选择边界（A_minus 偏大，μ−c 只有竞争块的一半），块质量几乎全部来自一个尖峰，而尖峰对块均值几乎没有贡献（去掉它之后均值只降 0.07）。这不是块内正负 logit 相消，而是均值只看得到偏低的背景。

**E3：RoPE 频带**
- **高频被池化平均掉，与理论一致。** 64 个频率对中 17 个属于高频带（a_f(128) < 0.5）。post-RoPE 后高频带的实测衰减 ρ = 0.10–0.12，理论 0.094；pre-RoPE 为 0.76–0.83。
- **低频带的衰减来自内容，与 RoPE 无关。** 低频带 post ≈ pre ≈ 0.63–0.80，理论值是 0.995。
- **L14 的漏选与 RoPE 高频无关。** missed 块尖峰优势 Δ 中，高频带贡献 1.3%（hit 为 4.7%），低频带贡献 98%。L0 的高频份额较高（26–41%），但 missed 块低于 hit 块。
- 图：`figures/e3_attenuation.png`、`figures/e3_band_contributions.png`

**E4：去 RoPE 的反事实**
- L14 被漏选的尖峰以**内容驱动**为主：RULER 42/50、LongBench 48/51。去掉 RoPE 后 98% 仍是块内 top-1，Δ_nope/Δ_rope 的中位数为 0.71–0.75。
- 三个已解码案例在所有 tile 上都是内容驱动，Δ_nope ≥ Δ_rope。
- L0 则以「混合」和「位置驱动」为主（36 + 12 / 50）。

## 4. Q3：尖峰 token 是否 sink 类（E5）

| 层 | sink 类占比（query 无关比例 > 0.8） | key 范数百分位 | value 范数百分位 | 出现为尖峰的 (层, 头) 数 |
| --- | ---: | ---: | ---: | ---: |
| RULER L14 | 7/7 = 100% | 0.7% | **0.0%** | 28 |
| LongBench L14 | 10/16 = 62.5%（组内 argmax 中位数 0.97） | 4.3% | **0.0%** | 26 |
| 其他层 | 0–14% | 8–95% | 4–85% | 1–3 |

- **L14 的尖峰是次级 attention sink。**
  - 对所有采样行、同一 KV 组的 7 个头，它都是所在块的 argmax。
  - key 范数低，value 几乎为零。
  - token 本身是普通文本片段：` won`、` don`（来自 won't / don't）、`/h`、`1`、`HE`、`M`、`(chunk`。
- 这类 sink 具有位置复现性：在同一样本的多个 tile、多个头上反复出现。
- 其他层的尖峰不是 sink 类，value 范数正常或偏高。

## 5. Q4：对输出的影响（E6）

L14，按采样行和头统计：

| 方案 | 质量 regret (pp) | rel_err 中位数 | rel_err p90 | oproj 误差中位数 | 占残差流的比例 |
| --- | ---: | ---: | ---: | ---: | ---: |
| C*（RULER / LB） | 0 / 0 | 0.067 / 0.080 | 0.35 / 0.30 | 0.103 / 0.101 | 3.4% / 3.9% |
| MeanPool | 7.53 / 7.89 | 0.139 / 0.142 | 0.53 / 0.46 | 0.155 / 0.158 | 4.8% / 6.1% |
| MeanPool + V2 补偿 | — | 0.130 / 0.134 | 0.51 / 0.44 | 0.148 / 0.152 | 4.6% / 5.9% |
| 真实块指数和 | 0.86 / 1.98 | 0.072 / 0.089 | 0.38 / 0.34 | 0.113 / 0.115 | 3.6% / 4.5% |

- **输出误差与质量损失同量级。** MeanPool 在 L14 的 head 输出误差约为 C* 的 2 倍；Spearman(行质量 regret, rel_err) = 0.76 / 0.65。「输出误差远小于质量 regret 所暗示的量级」这条预测**不成立**。
- **误差不以缩放为主。** |e_par| > e_perp 的行只占 47% / 31%，e_par 中位数 −0.02、e_perp 中位数 0.10。「主要是缩放」这条预测在汇总层面**不成立**。
- **按漏选块类型拆开，sink 块最便宜。**
  - L14 被漏选的质量中，sink 类尖峰块占 **82%（RULER）/ 55%（LB）**。
  - 这些块每单位质量造成的单块删除误差为 1.99 / 1.92；其他尖峰块为 14.0 / 6.2，分散块为 5.3 / 3.9。
  - 所以按质量排序会高估丢掉 sink 块的代价，但不能说丢掉它们无害：尖峰 token 的 v≈0，块内其余 15–40% 质量对应的 value 正常，而这些头的输出范数本身就小。
- **V2 均值补偿（离线仿真）**
  - L14：rel_err 中位数下降约 0.008。
  - L7 / L21 / L27：改善更明显，例如 RULER L7 从 0.125 降到 0.091。
  - L0：变差。均值 0.088 → 0.131，残差流占比 0.26 → 0.49。
  - 「V2 补不回尖峰块」与 L14 收益小的结果一致，但本轮没有逐块验证。
- 图：`figures/e6_mass_vs_output_error.png`

## 6. Q5：RBS 半径补救（E7）

RBS 按 arXiv 2609.20971 Eq. 4–14 实现，是近似实现，差异见 §8。

**固定预算（远端共 16 块，其中 k_rescue 块走补救分支）**

| L14（RULER / LB） | regret (pp) | missed 召回（按质量加权） | 补救块中不在 C* 的块数 | rel_err 中位数 |
| --- | ---: | ---: | ---: | ---: |
| k_rescue = 0（MeanPool） | 7.53 / 7.89 | 0 / 0 | — | 0.139 / 0.142 |
| k_rescue = 2 | 7.90 / 8.45 | 0.03 / 0.04 | 1.65 / 1.66 | 0.143 / 0.147 |
| k_rescue = 4 | 8.38 / 9.20 | 0.07 / 0.08 | 3.23 / 3.22 | 0.148 / 0.154 |
| k_rescue = 8 | 10.03 / 11.04 | 0.11 / 0.14 | 6.37 / 6.27 | 0.166 / 0.169 |

**阈值模式**
- 以 α_base = 0.08 为基准，加入 α_rescue = 0.18 的补救分支后：远端块数增加 1.6 / 1.5，其中 1.1 / 1.2 块不在 C*；regret 只下降 0.59 / 0.56 pp，missed 召回提高约 2 个百分点。
- 作为对照：在 MeanPool 排名上从 13 块加到 16 块，regret 下降 3.9 / 1.9 pp。所以补救分支每多花一块，换来的收益低于 MeanPool 排名上的下一块。
- 论文默认阈值 α_base = 0.22 / α_rescue = 0.18 在这里只选 6.2 / 6.4 块，远比本实验的预算稀疏，不能直接比较。

**三个已解码案例**
- 补救分支在所有配置下都没有捞回它们。
- K@9984 在一个 tile 上被 α = 0.08 的基础分支本身选中。

**原因（推断）**：sink 类尖峰的 key 范数处于最低的 1–4%，不是半径意义上的离群点，所以半径上界找不到它。

## 7. Q6：真实证据块（E8，仅 RULER）

RULER capture 保存了完整 Q，因此不需要 G1 补捕获。对最后一个 Q tile（问题 token 所在的位置），被查询的 needle 块在 L14 上：

| 指标 | 数值 |
| --- | --- |
| 进入 C* | 65% |
| 进入 MeanPool | 16% |
| 进入 V1 式阈值 α=0.08（离线仿真） | 7% |
| MeanPool 排名中位数 / 真实质量排名中位数 | 71.5 / 10 |
| g / N_eff / top-1 份额 | 6.4 / 3.5 / 0.56 |
| 块内峰值落在答案数字 span 内的比例 | 3% |

- 在 L21，needle 块进入 MeanPool 的比例为 35%，进入 C* 为 45%。
- 「needle 块的 g 小、在 MeanPool 下排名靠前、不会被漏」这条预测**不成立**。
- **但这不影响 V1 的实际答题。** V1 的配置是 `last_full_blocks=2`。已在 capture 的真实 V1 路由中核对：3 条样本的问题都从第 224 块开始（共 225 块），最后 2 个 Q 块在所有头上都是全因果 dense，上下文中段块的密度约 18%。
- **推断**：FlashPrefill 在 RULER 上能保住精度，一个直接原因是作答所依赖的查询行根本没有被稀疏化。选块误差只能通过中段 token 的表示（后续层的 K/V）间接传到答案。这正是 G3（误差传播）要量化的东西。
- **尚未分析**：needle 块内的峰值 token 是什么（97% 不在答案数字上），是否与 E5 的 sink token 重合。

## 8. 近似实现与限制

**近似实现**
- **RBS**：
  - 用 post-RoPE 的 key（论文没有说明）；
  - 半径分位数按「整个 prompt、每层、每个 KV 头的全部完整块」计算；
  - 每个 tile 只用 16 个采样行；
  - 固定预算变体是我的离线改编。
- **V2 补偿**：按 FlashPrefillv2 `75b58f2` 的 kernel 逻辑用 float64 仿真，k̄、v̄ 为取整到 BF16 的普通 token 均值。
- **`thr_base_a*`**：离线的 V1 式相对阈值，用 16 行，真实 V1 用全部 128 行。

**限制**
- RULER 只有 3 条样本，且共用同一背景文本；LongBench 只有 8 条。
- 所有指标都是冻结 Q/K/V 下的单层诊断，不代表层间误差传播，也不代表任务准确率。
- E2 的稀释判据在线性空间计算，对 exp 空间的尖峰不敏感（见 §3）。
- E5 用的查询行是 Q = 64/128/256 各 tile 的采样行，每个位置约 99–106 行。

**E2 用到的术语**
- A_plus / A_minus：块内 token 高于 / 低于参考水平 c 的平均幅度。
- Cliff's δ：两组分布重叠程度的效应量，范围 −1 到 1。

## 9. 复现

```bash
cd /root/autodl-tmp/exp3 && git pull --ff-only
bash run_block_proxy_validation.sh --capture results/longbench_block_proxy_capture_remaining_20260925_002508_812453892
```

依赖：torch、pandas、matplotlib、safetensors、huggingface_hub（远端 exp1 环境已具备，不需要 scipy）。
